{
  "name": "shortener-service",
  "version": "1.0.0",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/main.jsx --bundle --outfile=public/dist.js --minify"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230807.0",
    "@cloudflare/wrangler": "^3.14.0",
    "esbuild": "^0.19.2"
  }
}
